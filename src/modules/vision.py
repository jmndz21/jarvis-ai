"""Día 5: visión por cámara.

Pendiente de implementar según TASKS.md:
- capture_frame() -> bytes          # captura un frame con OpenCV
- describe_scene(frame: bytes) -> str  # envía el frame a un modelo multimodal
- detect_faces(frame: bytes) -> list
"""
