"""Día 4: integración con Gmail API.

Pendiente de implementar según TASKS.md:
- authenticate() -> Credentials   # OAuth2, guarda token localmente (no en el repo)
- fetch_recent(n: int = 10) -> list[dict]
- summarize(messages: list[dict]) -> str
- draft_reply(message: dict, instructions: str) -> str  # requiere confirmación antes de enviar
"""
