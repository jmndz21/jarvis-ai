"""Día 2: reconocimiento de voz (STT), síntesis (TTS) y wake word.

Pendiente de implementar según TASKS.md:
- listen() -> str          # graba audio y lo transcribe con Whisper
- speak(text: str) -> None # sintetiza y reproduce audio (ElevenLabs/Piper)
- wait_for_wake_word()     # bloquea hasta escuchar la palabra de activación
"""
