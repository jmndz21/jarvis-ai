# Copia este archivo a .env y ajusta si quieres cambiar valores por defecto.
# Ollama corre 100% local: no necesita ninguna API key.

JARVIS_MODEL=llama3.2
JARVIS_NAME=Jarvis

# Se agregará más adelante según TASKS.md:
# GMAIL_CLIENT_SECRET_PATH=   (Día 4 — Piper para voz es local y no necesita API key)
